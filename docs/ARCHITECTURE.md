# 🏗️ OpenKlaw 시스템 아키텍처

> **Version:** 0.1 MVP  
> **최종 업데이트:** 2025-02-07

---

## 📐 전체 아키텍처

```
┌──────────────────────────────────────────────────────────────────────┐
│                           OpenKlaw Desktop                            │
│                                                                        │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │                      Presentation Layer                         │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │  │
│  │  │  System     │  │   Web UI    │  │    CLI      │            │  │
│  │  │  Tray       │  │  (SvelteKit)│  │  Interface  │            │  │
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘            │  │
│  └─────────┼────────────────┼────────────────┼────────────────────┘  │
│            │                │                │                        │
│            └────────────────┴────────────────┘                        │
│                             │                                          │
│  ┌──────────────────────────▼──────────────────────────────────────┐  │
│  │                      Application Layer                           │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │  │
│  │  │   Tauri     │  │   Session   │  │   Config    │             │  │
│  │  │   Bridge    │  │   Manager   │  │   Manager   │             │  │
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘             │  │
│  └─────────┼────────────────┼────────────────┼─────────────────────┘  │
│            │                │                │                        │
│  ┌─────────▼────────────────▼────────────────▼─────────────────────┐  │
│  │                       Core Layer                                 │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │  │
│  │  │   Skills    │  │   Tools     │  │   Memory    │             │  │
│  │  │   Engine    │  │   Runtime   │  │   Store     │             │  │
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘             │  │
│  └─────────┼────────────────┼────────────────┼─────────────────────┘  │
│            │                │                │                        │
│  ┌─────────▼────────────────▼────────────────▼─────────────────────┐  │
│  │                    Integration Layer                             │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │  │
│  │  │   Ollama    │  │   File      │  │   Python    │             │  │
│  │  │   Client    │  │   System    │  │   Bridge    │             │  │
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘             │  │
│  └─────────┼────────────────┼────────────────┼─────────────────────┘  │
└────────────┼────────────────┼────────────────┼────────────────────────┘
             │                │                │
             ▼                ▼                ▼
      ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
      │   Ollama    │  │ Local Files │  │   pyhwp     │
      │   Server    │  │ (HWP, XLSX) │  │ (Python)    │
      └─────────────┘  └─────────────┘  └─────────────┘
```

---

## 🧱 Layer 상세

### 1. Presentation Layer (프레젠테이션 계층)

사용자 인터페이스 담당

| 컴포넌트 | 기술 | 역할 |
|----------|------|------|
| **System Tray** | Tauri tray | 백그라운드 실행, 빠른 접근 |
| **Web UI** | SvelteKit | 메인 채팅 인터페이스 |
| **CLI Interface** | Node.js | 터미널 명령어 지원 |

### 2. Application Layer (애플리케이션 계층)

비즈니스 로직 및 상태 관리

| 컴포넌트 | 기술 | 역할 |
|----------|------|------|
| **Tauri Bridge** | Rust | 네이티브 ↔ 웹 통신 |
| **Session Manager** | TypeScript | 대화 세션 관리 |
| **Config Manager** | TypeScript | 설정 저장/로드 |

### 3. Core Layer (코어 계층)

핵심 AI 기능

| 컴포넌트 | 기술 | 역할 |
|----------|------|------|
| **Skills Engine** | TypeScript | 스킬 실행 (이메일, 요약 등) |
| **Tools Runtime** | TypeScript | 도구 실행 (파일 읽기 등) |
| **Memory Store** | SQLite | 대화 기록, 문서 인덱싱 |

### 4. Integration Layer (통합 계층)

외부 시스템 연동

| 컴포넌트 | 기술 | 역할 |
|----------|------|------|
| **Ollama Client** | TypeScript | Ollama API 호출 |
| **File System** | Node.js | 로컬 파일 읽기/쓰기 |
| **Python Bridge** | child_process | pyhwp 등 Python 도구 호출 |

---

## 📁 디렉토리 구조

```
OpenKlaw/
├── src/
│   ├── tauri/                 # Tauri (Rust) 백엔드
│   │   ├── src/
│   │   │   ├── main.rs        # 앱 진입점
│   │   │   ├── tray.rs        # 시스템 트레이
│   │   │   ├── commands.rs    # Tauri 명령어
│   │   │   └── lib.rs         # 라이브러리
│   │   ├── Cargo.toml
│   │   └── tauri.conf.json    # Tauri 설정
│   │
│   ├── web/                   # SvelteKit 프론트엔드
│   │   ├── src/
│   │   │   ├── lib/
│   │   │   │   ├── components/   # UI 컴포넌트
│   │   │   │   ├── stores/       # Svelte 스토어
│   │   │   │   └── utils/        # 유틸리티
│   │   │   └── routes/           # 페이지 라우트
│   │   ├── static/
│   │   ├── svelte.config.js
│   │   └── package.json
│   │
│   └── core/                  # 핵심 로직 (공유)
│       ├── ollama/            # Ollama 클라이언트
│       ├── skills/            # 스킬 정의
│       ├── tools/             # 도구 정의
│       └── memory/            # 메모리 관리
│
├── scripts/                   # 빌드/설치 스크립트
│   ├── install-ollama.ps1     # Windows Ollama 설치
│   ├── install-ollama.sh      # Unix Ollama 설치
│   └── post-install.js        # 설치 후 작업
│
├── bin/                       # CLI 실행 파일
│   └── openklaw.js            # npm global 진입점
│
├── docs/                      # 문서
├── .ralph/                    # Ralph 에이전트 설정
├── package.json               # npm 패키지 설정
└── README.md
```

---

## 🔌 주요 인터페이스

### Ollama API

```typescript
interface OllamaClient {
  // 모델 목록
  listModels(): Promise<Model[]>;
  
  // 채팅 완성
  chat(params: {
    model: string;
    messages: Message[];
    stream?: boolean;
  }): AsyncGenerator<ChatResponse>;
  
  // 모델 다운로드
  pullModel(name: string): AsyncGenerator<PullProgress>;
  
  // 헬스체크
  isHealthy(): Promise<boolean>;
}
```

### Skills Engine

```typescript
interface Skill {
  name: string;
  description: string;
  
  // 스킬 실행 가능 여부
  canHandle(input: string): boolean;
  
  // 스킬 실행
  execute(params: {
    input: string;
    context: Context;
    llm: OllamaClient;
  }): Promise<SkillResult>;
}

// 내장 스킬
type BuiltinSkills = 
  | 'email'      // 이메일 작성
  | 'summarize'  // 문서 요약
  | 'translate'  // 번역
  | 'analyze';   // 데이터 분석
```

### Tools Runtime

```typescript
interface Tool {
  name: string;
  description: string;
  parameters: JSONSchema;
  
  // 도구 실행
  execute(params: Record<string, unknown>): Promise<ToolResult>;
}

// 내장 도구
type BuiltinTools =
  | 'read_file'     // 파일 읽기
  | 'write_file'    // 파일 쓰기
  | 'parse_hwp'     // HWP 파싱
  | 'parse_excel';  // Excel 파싱
```

### Tauri Commands

```rust
// Rust → JavaScript 호출 가능 명령어
#[tauri::command]
fn send_message(message: String) -> Result<String, String>;

#[tauri::command]
fn get_history() -> Result<Vec<Message>, String>;

#[tauri::command]
fn check_ollama() -> Result<bool, String>;

#[tauri::command]
fn get_system_info() -> Result<SystemInfo, String>;
```

---

## 💾 데이터 저장

### 저장 위치

```
~/.openklaw/
├── config.json          # 설정 파일
├── history.db           # SQLite - 대화 기록
├── documents/           # 임시 문서 저장
└── logs/                # 로그 파일
```

### 대화 기록 스키마 (SQLite)

```sql
CREATE TABLE sessions (
  id TEXT PRIMARY KEY,
  title TEXT,
  created_at DATETIME,
  updated_at DATETIME
);

CREATE TABLE messages (
  id TEXT PRIMARY KEY,
  session_id TEXT REFERENCES sessions(id),
  role TEXT,  -- 'user' | 'assistant'
  content TEXT,
  created_at DATETIME
);

CREATE VIRTUAL TABLE documents USING fts5(
  title,
  content,
  file_path
);
```

---

## 🔄 데이터 흐름

### 채팅 요청 흐름

```
User Input
    │
    ▼
┌─────────────┐
│  Web UI     │ ─── JavaScript event ───▶
└─────────────┘
                                          │
                                          ▼
                               ┌─────────────────────┐
                               │  Tauri Bridge       │
                               │  (invoke command)   │
                               └──────────┬──────────┘
                                          │
                                          ▼
                               ┌─────────────────────┐
                               │  Session Manager    │
                               │  - 컨텍스트 구성    │
                               │  - 스킬 감지        │
                               └──────────┬──────────┘
                                          │
                      ┌───────────────────┼───────────────────┐
                      │                   │                   │
                      ▼                   ▼                   ▼
               ┌───────────┐       ┌───────────┐       ┌───────────┐
               │ Skill     │       │ Tool      │       │ Direct    │
               │ Engine    │       │ Runtime   │       │ Chat      │
               └─────┬─────┘       └─────┬─────┘       └─────┬─────┘
                     │                   │                   │
                     └───────────────────┼───────────────────┘
                                         │
                                         ▼
                               ┌─────────────────────┐
                               │  Ollama Client      │
                               │  (streaming)        │
                               └──────────┬──────────┘
                                          │
                                          ▼
                               ┌─────────────────────┐
                               │  Ollama Server      │
                               │  (localhost:11434)  │
                               └──────────┬──────────┘
                                          │
                                          ▼
                                    LLM Response
                                    (streamed)
```

---

## 🚀 배포 구조

### Windows (.exe 설치파일)

```
OpenKlaw-Setup.exe
├── OpenKlaw.exe           # 메인 앱 (Tauri)
├── ollama/                # 번들된 Ollama
│   └── ollama.exe
├── models/                # 사전 다운로드 모델 (선택)
│   └── qwen2.5-3b/
└── install.nsi            # NSIS 스크립트
```

### macOS (npm package)

```
openklaw (npm)
├── bin/
│   └── openklaw.js        # CLI 진입점
├── dist/
│   └── OpenKlaw.app       # macOS 앱 번들
├── scripts/
│   └── postinstall.js     # Ollama 설치 스크립트
└── package.json
```

---

## 🔐 보안 고려사항

### 데이터 보안

- ✅ 100% 로컬 처리 - 네트워크 요청 없음
- ✅ 대화 기록 로컬 저장 (SQLite)
- ✅ 설정 파일 사용자 홈 디렉토리에 저장
- 🔜 민감 데이터 암호화 저장 (P1)

### 실행 보안

- ✅ Tauri: Chromium보다 작은 공격 면적
- ✅ Python subprocess 샌드박싱
- ✅ 파일 접근 사용자 확인

---

## 📊 성능 목표

| 지표 | 목표 | 측정 방법 |
|------|------|----------|
| 앱 시작 | < 3초 | 트레이 아이콘 클릭 → 창 표시 |
| 첫 토큰 | < 5초 | 메시지 전송 → 첫 응답 |
| 메모리 | < 500MB | LLM 제외, 앱만 |
| 설치 크기 | < 50MB | LLM/Ollama 제외 |
